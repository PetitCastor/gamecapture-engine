namespace GameCapture.Sdk;

/// <summary>
/// Settings for an <c>"overlay"</c> <see cref="SinkSpec"/> — position/anchor/font/template/lingerMs.
/// Stub pending the overlay package (TASK-SINK-05); core only carries the shape so
/// <see cref="SinkSpec.Overlay"/> can bind from JSON today without a Windows dependency.
/// </summary>
public sealed class OverlaySpec;
